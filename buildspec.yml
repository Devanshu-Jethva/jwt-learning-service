version: 0.2

env:
  variables:
    DB_HOST_URL: "jdbc:postgresql://postgres-spring.c5uqyi884b2w.ap-south-1.rds.amazonaws.com:5432/postgres"
    DB_USERNAME: "postgres"
    DB_PASSWORD: "#70416DJd"
    DOCKER_USERNAME: "devanshu1309"
    DOCKER_PASSWORD: "#70416DJd"


phases:
  install:
    runtime-versions:
      java: corretto21
    commands:
      - echo Installing Maven...
  pre_build:
    commands:
      - echo Logging in to Docker hub...
      - echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
  build:
    commands:
      - echo Building, Testing and packaging the application...
      - mvn package
  post_build:
    commands:
      - echo Build, Testing and packaging completed.
artifacts:
  files:
    - target/*.jar
  discard-paths: yes
cache:
  paths:
    - '/root/.m2/**/*'
